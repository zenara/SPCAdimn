@model IEnumerable<AdminSiteMVC.Models.section>

@{
    ViewBag.Title = "Index";
}

<h2>Add New </h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.content)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sec_no)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.policy.p_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.version.v_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.content)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sec_no)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.policy.p_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.version.v_id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.sec_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.sec_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.sec_id })
        </td>
    </tr>
}
</table>
